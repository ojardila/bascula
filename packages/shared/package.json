{
  "name": "@bascula/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Domain rules shared by the web app and checked against the API: money, weeks, enums.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
